import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  
  imageWraper: {
    width:'100%',
    height:100,
    alignItems: 'center',
    borderRadius: 10,
    borderColor:"#d1d5da",
    borderWidth:1
}
});

export default styles;
